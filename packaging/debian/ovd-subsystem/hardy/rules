#!/usr/bin/make -f

include /usr/share/cdbs/1/rules/debhelper.mk

DEB_DH_INSTALLINIT_ARGS := -- "defaults 98 02"

install/ulteo-ovd-subsystem::
	CONFDIR=$(CURDIR)/debian/$(cdbs_curpkg)/etc/ulteo/ ; \
	mkdir -p $$CONFDIR ; \
	echo "CHROOT=" > $$CONFDIR/subsystem.conf

clean::
	#debconf-updatepo
