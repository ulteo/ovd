#! /bin/sh -e

. /usr/share/debconf/confmodule

if [ "$1" = "configure" ] && [ -z "$2" ] || [ "$1" = "reconfigure" ]
then
    db_input critical ulteo-ovd-slaveserver/session_manager || true
    db_go
fi

db_stop

#DEBHELPER#

exit 0
