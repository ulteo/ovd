#!/bin/sh -e

DEBCONFDB=/usr/share/ulteo/ovd/debconfdb

case "$1" in

    configure)
        debconf-set-selections /usr/share/ulteo/ovd/debconfdb
    ;;

    abort-*)
        exit 1
    ;;

    *) ;;
esac

#DEBHELPER#

exit 0
