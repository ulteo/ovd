#!/bin/sh -e

. /usr/share/debconf/confmodule

SMDIR=/usr/share/ulteo/sessionmanager
A2USER=www-data

# Check if update is possible
if [ -f $SMDIR/tools/can_update.php ]
then
   su -s /usr/bin/php $A2USER "$SMDIR/tools/can_update.php"
   [ $? -ne 0 ] && exit 1
fi

db_stop

#DEBHELPER#

exit 0
