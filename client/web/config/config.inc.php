<?php
// IP/Host of the SessionManager to link the Web Client to
// define('SESSIONMANAGER_HOST', '127.0.0.1');

// Public IP/Port of the Gateway (only use this if the WebClient is INSIDE a LAN and BEHIND the Gateway)
// define('GATEWAY_WAN_IP', '8.8.8.8');
// define('GATEWAY_WAN_PORT', 443);

// Private IP/Port of the Gateway (only use this if the WebClient is INSIDE a LAN and BEHIND the Gateway)
// define('GATEWAY_LAN_IP', '127.0.0.1');
// define('GATEWAY_LAN_PORT', 443);

// Enable/disable debug mode
define('DEBUG_MODE', 0);

// Select RDP input method
// define('RDP_INPUT_METHOD', 'scancode'); // default
// define('RDP_INPUT_METHOD', 'unicode');  // alternative method


// Perform desktop integration in portal sessions
//   publish destkop and start menu icons, mime type association, ...
// define('PORTAL_LOCAL_INTEGRATION', true); // default is false
