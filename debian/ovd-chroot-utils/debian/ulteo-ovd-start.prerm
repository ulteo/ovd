#!/bin/sh -e

if [ "$1" = "remove" ] || [ "$1" = "debconfigure" ]; then
    for alt in startovd-kde startovd-xfce; do
        update-alternatives --remove startovd /usr/bin/$alt
    done
fi

#DEBHELPER#

exit 0

