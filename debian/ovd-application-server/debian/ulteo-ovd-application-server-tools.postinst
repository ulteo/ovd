#!/bin/sh -e

[ "$1" != "configure" ] && exit 0

# set aps-shell setuid root
if ! dpkg-statoverride --list /usr/bin/aps-shell >/dev/null 2>&1; then
    dpkg-statoverride --update --add root root 4755 /usr/bin/aps-shell
fi

#DEBHELPER#

exit 0
